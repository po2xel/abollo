#include "Market/Model/Table.h"

#include <thrust/device_vector.h>
#include <thrust/host_vector.h>



namespace abollo
{

// template <typename T, const std::size_t P, ColumnType Y>
// using HostArray = ChunkedArray<thrust::host_vector, T, P, Y>;
//
//
// template <typename T, const std::size_t Cap, ColumnType Y>
// using DeviceArray = ChunkedArray<thrust::device_vector<T>, Cap, Y>;
//
//
//
// template <typename T, const std::size_t P, ColumnType... Ys>
// class MarketingTable : public Table<thrust::device_vector<T>, P, Ys...>
// {
// };

}    // namespace abollo